import React from 'react';

export const ConversationOver = props => (
  <section className="conversation-over-notify">
    The conversation has been marked as finished by either of the parties. The messages will stay until you navigate
    to another chat.
  </section>
);